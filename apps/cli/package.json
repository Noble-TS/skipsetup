{
  "name": "skipsetup-cli",
  "version": "0.1.4",
  "type": "module",
  "bin": {
    "skipsetup": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "scripts": {
      "build": "pnpm -F @skipsetup/core build && pnpm -F @skipsetup/plugin-small build && pnpm -F @skipsetup/plugin-medium build && tsc --build",
  "prepublishOnly": "pnpm run build",
    "type-check": "tsc --noEmit -p tsconfig.tests.json",
    "lint": "eslint src --ext .ts --fix",
    "test": "vitest --config vitest.config.ts"
  },
  "dependencies": {
    
    "clipanion": "^3.2.1",
    "figlet": "^1.9.4",
    "gradient-string": "^3.0.0",
    "js-yaml": "^4.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^2.0.0"
  },
  "typesVersions": {
    ">=4.5": {
      "*": [
        "dist/*"
      ]
    }
  }
}